/* exported last */

// dec func
// dec string var last 
//Find the length of the array
// Assign last index of array to var last
// return var last

function last(array){
  var last = ''; 
  var lastIndex= array.length - 1;  
  last = array[lastIndex];
  return last; 
}
